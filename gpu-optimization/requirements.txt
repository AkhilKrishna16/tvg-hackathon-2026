# Core numerical computing
numpy>=1.24
scipy>=1.10

# GPU acceleration (optional — falls back to NumPy if unavailable)
# cupy-cuda12x>=12.0   # Uncomment and pin to your CUDA version

# Visualization
matplotlib>=3.7

# HTTP client (used by fetch_real_data.py for OSM/Census APIs)
requests>=2.28

# Development / testing
pytest>=7.4
pytest-benchmark>=4.0
